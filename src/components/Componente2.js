import React from 'react';

// function Header (props) {
//   return ( <h1>Hola soy un componente funcional con extención JS y tengo un {props.msg} </h1> );
// }

const Componente2 = (props) => {
  return ( <h2>Hola soy un componente funcional con extención JS y tengo un {props.msg} </h2>  );
}
 
export default Componente2;
 
